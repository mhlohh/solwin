services:
  ml-service:
    build: .
    ports: ["8000:8000"]
    env_file:
      - path: .env
        required: false
    environment:
      MODEL_DIR: /app/models
    volumes:
      - ./models:/app/models:ro
    read_only: true
    tmpfs: [/tmp]
